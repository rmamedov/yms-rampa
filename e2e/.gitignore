# Артефакти прогону: трасування, скріншоти, звіт. Вага велика, користі в
# історії немає — потрібні лише під час розбору конкретного падіння.
test-results/
playwright-report/
results.json

# Знімки, зроблені screenshots.mjs — у репозиторії не потрібні.
screenshots/
